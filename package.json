{
  "name": "gemini-image-tool",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "node src/generate-image.js",
    "ui": "node src/server.js",
    "check": "npm run check:syntax && npm run check:architecture",
    "check:syntax": "node scripts/check-js-syntax.mjs",
    "check:architecture": "node scripts/check-architecture.mjs",
    "test:smoke": "node scripts/smoke-ui.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
